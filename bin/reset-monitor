#!/usr/bin/env bash

lvds=$(xrandr -q | sed -n 's/^\(LVDS[^ ]\+\) connected .*/\1/p')
vga=$(xrandr -q | sed -n 's/^\(VGA[^ ]\+\) connected .*/\1/p')
xrandr --output $vga --auto --above $lvds
xrandr --output $lvds --auto
